# V3.3 实施端验证记录

验证日期：2026-07-30
实施基线：`83c27c0ad0496ecdab211f359ca86b741231867e`

## 执行命令

```powershell
python -m compileall -q core layout tools tests run_demo.py app.py
python -m unittest discover -s tests -v
python tools/pic_mcp_client.py --smoke-test
python tools/run_prompt_benchmark.py
python run_demo.py
python tools/check_latest_run.py
```

Streamlit 使用 `streamlit.testing.v1.AppTest` 执行无界面启动检查。

## 验证结果

- Python 编译检查：通过。
- 单元与集成测试：19 项通过。
- MCP Client：成功列出并调用三个工具。
- Prompt Benchmark：30 条需求，解析准确率 1.0，Schema 通过率 1.0，端到端成功率 1.0。
- 命令行完整流程：通过。
- 最新运行输出完整性检查：通过。
- Streamlit AppTest：0 个异常。
- `git diff --check`：通过。

## V3.2 基准保持检查

与本地 V3.2 代表性运行 `run_20260710_170210` 对比：

| 指标 | V3.2 基准 | V3.3 验证 |
| --- | ---: | ---: |
| 最优 MMI 宽度 / μm | 2.449367088607595 | 2.449367088607595 |
| 最优 MMI 长度 / μm | 5.306532663316583 | 5.306532663316583 |
| MMI 使用 neff | 2.741485173411228 | 2.7414851734112347 |
| overlap 总功率 | 0.8433501567731183 | 0.8433501567731166 |

差异仅为浮点舍入量级。V3.3 没有修改模式求解、MMI 优化、BPM、Gaussian 端口模式或 overlap 物理算法。

## 已知边界

- Benchmark 是当前确定性规则解析器的公开回归集，不代表开放域自然语言理解能力。
- 本地 MCP 层只暴露受控工具，不接入外部 LLM API。
- 当前模式与传播仍是二维标量近似。
- Gaussian overlap 不是严格全矢量端口本征模式 S 参数。
